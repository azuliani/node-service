# CLAUDE.md

WebSocket-based messaging library for Node.js. See **SPEC.md** for complete specification.

## Quick Reference

```bash
npm install           # Install dependencies
npm test              # Run test suite
npm run typecheck     # Type check (tsc --noEmit)
npm run lint          # ESLint
npm run build         # Compile to dist/
```

Node.js 23.6+ required. No build step for development—TypeScript runs directly via native type stripping.

## Git Commits

Do not add "Co-Authored-By" lines to commit messages.
